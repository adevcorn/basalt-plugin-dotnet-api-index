[package]
name = "dotnet-api-index"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
basalt-plugin-sdk = { path = "../basalt-plugin-sdk" }
regex = "1.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
